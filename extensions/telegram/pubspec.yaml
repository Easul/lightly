name: telegram_native_dependency_fetcher
description: "Fetches the pinned TDLib native binary used by Lightly's Android companion."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  tdlib: ^1.6.0
